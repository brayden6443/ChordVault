{
  "name": "chord-vault",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.ts",
    "smoke:build": "node scripts/smoke-build.mjs",
    "db:migrate:local": "wrangler d1 migrations apply chord-vault --local",
    "db:status:local": "wrangler d1 migrations list chord-vault --local",
    "db:migrate:production": "wrangler d1 migrations apply chord-vault --remote",
    "worker:check": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build && npm run smoke:build"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^24.10.0",
    "eslint": "^9.39.2",
    "globals": "^16.5.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0",
    "vite": "^7.0.0",
    "wrangler": "^4.118.0"
  }
}
